#include "zephyr/dt-bindings/gpio/gpio.h"

/ {
    interrupt-loop {
        compatible = "zig2025,gpio-loop";
        in-gpios = <&gpio0 1 (GPIO_ACTIVE_HIGH)>;
        out-gpios = <&gpio0 2 (GPIO_ACTIVE_HIGH)>;
    };
};
